#include "math_stuff.h"

//Yea, we could have just coded return a+b, but for demo purposes
//we are going to be a bit long winded.
int my_add(int a, int b){
    int sum;

    sum = a+b;
    return sum;
}